int main(){
    // Test unknown
    auto unknown;

    // Test type propagation
    auto propagated = 0;

    int regularInt = 0;

    // Test ReferenceType chain
    int& ref = regularInt;

    // Test PointerType chain
    int* ptr = &regularInt;

}